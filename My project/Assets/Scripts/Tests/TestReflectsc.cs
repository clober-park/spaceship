using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestReflectsc : MonoBehaviour
{
    public Plane Plane;
    public Wall wall;
    // Start is called before the first frame update
    void Start()
    {
        //Debug.DrawRay(Plane.transform.position, Plane.Direct, Color.red);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
